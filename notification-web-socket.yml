spring:
  application:
    name: notification-web-socket
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8098
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
